{
  "$schema": "https://railway.app/railway.schema.json",
  "_comment": "This file is intentionally minimal. The runner service on Railway should set Root Directory = `runner`, which makes runner/railway.json the active config. Next.js deploys to Vercel separately and doesn't need a railway.json."
}
